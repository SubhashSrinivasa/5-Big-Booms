import React from 'react';
import './Navbar.css'; // Optional: Add styles for the Navbar

const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="logo">fax</div>
            {/* ...removed links... */}
        </nav>
    );
};

export default Navbar;